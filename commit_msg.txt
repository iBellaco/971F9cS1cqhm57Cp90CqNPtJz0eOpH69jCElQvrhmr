git commit -a -m "fix(scanner): mejora drásticamente el pipeline visual sincronizando los recortes de hash, previene sobreescrituras OCR en slots inactivos y corrige la asignación de rol natural en enemigos"
