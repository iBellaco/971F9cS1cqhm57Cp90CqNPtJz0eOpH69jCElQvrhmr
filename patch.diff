--- app/src/main/java/com/example/ui/screens/MetaAndDraftScreen.kt
+++ app/src/main/java/com/example/ui/screens/MetaAndDraftScreen.kt
@@ -329,3 +329,9 @@
                         enemyLaneOpponent = enemyLaneOpponent,
-                        onToggleFirstPick = { isFirstPick = !isFirstPick },
+                        onToggleFirstPick = { 
+                            if (activeRole == null) {
+                                android.widget.Toast.makeText(context, "Debes seleccionar tu línea primero", android.widget.Toast.SHORT).show()
+                            } else {
+                                isFirstPick = !isFirstPick 
+                            }
+                        },
                         onChangeRole = { showRoleChangeDialog = true },
@@ -520,3 +526,9 @@
                         enemyLaneOpponent = enemyLaneOpponent,
-                        onToggleFirstPick = { isFirstPick = !isFirstPick },
+                        onToggleFirstPick = { 
+                            if (activeRole == null) {
+                                android.widget.Toast.makeText(context, "Debes seleccionar tu línea primero", android.widget.Toast.SHORT).show()
+                            } else {
+                                isFirstPick = !isFirstPick 
+                            }
+                        },
                         onChangeRole = { showRoleChangeDialog = true },
